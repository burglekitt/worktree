import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  branch: "branch",
  checkout: "checkout",
  config: "config",
  list: "list",
  open: "open",
  remove: "remove",
  cleanup: "cleanup",
};

export default meta;
